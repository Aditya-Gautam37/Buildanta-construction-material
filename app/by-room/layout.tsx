import type { Metadata } from "next";
export const metadata: Metadata = { title: "Shop Materials by Room", description: "Browse construction and interior materials for kitchens, bathrooms, bedrooms, living rooms and outdoor spaces." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
