import type { Metadata } from "next";
export const metadata: Metadata = { title: "Buildanta Services", description: "Explore Buildanta professional discovery, bulk quotes, supplier listings and inventory services." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
