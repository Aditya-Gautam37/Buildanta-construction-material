import type { Metadata } from "next";
export const metadata: Metadata = { title: "Request Bulk Construction Quotes", description: "Send your construction-material requirement and request project pricing from the Buildanta team." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
