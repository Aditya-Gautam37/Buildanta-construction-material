import type { Metadata } from "next";
export const metadata: Metadata = { title: "List Construction Products", description: "Submit construction products for review and catalogue listing through Buildanta." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
