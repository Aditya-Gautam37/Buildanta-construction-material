import type { Metadata } from "next";
export const metadata: Metadata = { title: "Shop Materials by Construction Stage", description: "Find Buildanta construction materials for foundation, masonry, waterproofing, flooring, electrical and finishing stages." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
